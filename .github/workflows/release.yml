name: Build pyMSscreen App

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-platform:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        arch: [x64]
    runs-on: ${{ matrix.os }}
    continue-on-error: true
    name: Build (${{ matrix.os }})
    timeout-minutes: 90

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Micromamba
        uses: mamba-org/setup-micromamba@v1
        continue-on-error: true
        with:
          environment-file: environment.core.yml
          environment-name: backend
          init-shell: bash
          cache-environment: true
          cache-downloads: true
          cache-environment-key: env-${{ matrix.os }}-${{ hashFiles('environment.core.yml') }}
          cache-downloads-key: pkgs-${{ matrix.os }}-${{ hashFiles('environment.core.yml') }}

      - name: Install Python dependencies
        shell: bash
        run: |
          micromamba run -n backend pip install -r requirements.txt
          micromamba run -n backend pip install pyinstaller
          echo "🔍 Installing pyopenms per platform..."
          if [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
             micromamba install -n backend -c bioconda -c conda-forge pyopenms
          elif [[ "${{ matrix.os }}" == "windows-latest" ]]; then
             micromamba run -n backend pip install wheels/pyopenms-3.3.0-cp312-cp312-win_amd64.whl             
          elif [[ "${{ matrix.os }}" == "macos-latest" ]]; then
             micromamba run -n backend pip install wheels/pyopenms-3.3.0-cp312-cp312-macosx_14_0_arm64.whl
          else
             echo "⚠️ pyopenms not installed: unknown platform"
          fi

      - name: Build Backend Binary
        shell: bash
        run: |
          echo "📦 Building PyInstaller binary for: ${{ matrix.os }}"
          
          if [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            echo "🛠️ macOS build with dynamic shared libraries"
            WHEEL_LIB_PATH="${MAMBA_ROOT_PREFIX:-$HOME/micromamba}/envs/backend/lib/python3.12/site-packages/pyopenms"

            ADD_BINARIES=""
            echo "🔍 Scanning for dylibs in: $WHEEL_LIB_PATH"
            for lib in "$WHEEL_LIB_PATH"/*.dylib; do
              if [[ -f "$lib" ]]; then
                echo "✅ Adding $(basename "$lib")"
                ADD_BINARIES+=" --add-binary $lib:."
              fi
            done

            OPENMS_DATA_PATH="$WHEEL_LIB_PATH/share/OpenMS"
            echo "📁 OpenMS data path: $OPENMS_DATA_PATH"

            micromamba run -n backend pyinstaller --clean --log-level DEBUG \
              --onefile --name web_app web_app.py $ADD_BINARIES \
              --add-data "$OPENMS_DATA_PATH:share/OpenMS"

          elif [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            echo "🛠️ Windows build with specified pyopenms DLLs"
            WHEEL_LIB_PATH="${MAMBA_ROOT_PREFIX:-$HOME/micromamba}/envs/backend/Lib/site-packages/pyopenms"

            ADD_BINARIES=""
            for dll in \
              OpenMS.dll \
              OpenSwathAlgo.dll \
              Qt5Core.dll \
              Qt5Network.dll \
              concrt140.dll \
              msvcp140.dll \
              msvcp140_1.dll \
              msvcp140_2.dll \
              msvcp140_atomic_wait.dll \
              msvcp140_codecvt_ids.dll \
              vcruntime140.dll \
              vcruntime140_1.dll \
              vcomp140.dll; do

              DLL_PATH="$WHEEL_LIB_PATH/$dll"
              if [[ -f "$DLL_PATH" ]]; then
                echo "✅ Adding $dll"
                ADD_BINARIES+=" --add-binary $DLL_PATH:."
              else
                echo "⚠️ Missing $dll"
              fi
            done

            micromamba run -n backend pyinstaller --clean --log-level DEBUG \
              --onefile --name web_app web_app.py $ADD_BINARIES

          else
            echo "🛠️ Standard build (no extra binaries)"
            micromamba run -n backend pyinstaller --clean --log-level DEBUG \
              --onefile --name web_app web_app.py
          fi

          mkdir -p public/backend
          if [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
            mv dist/web_app public/backend/web_app_linux
          elif [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            mv dist/web_app public/backend/web_app_macos
          elif [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            mv dist/web_app.exe public/backend/web_app.exe
          fi

      - name: Make Backend Executable (Non-Windows)
        if: matrix.os != 'windows-latest'
        run: chmod +x public/backend/web_app_*

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ matrix.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ matrix.os }}-node-

      - name: Install Frontend Dependencies
        run: npm ci

      - name: Install Playwright (required for tests)
        run: npx playwright install --with-deps

      - name: Build React App
        run: npm run build

      - name: Launch Backend
        run: |
          if [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            ./public/backend/web_app_macos &
          elif [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
            ./public/backend/web_app_linux &
          elif [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            start /B public\\backend\\web_app.exe
          fi
          sleep 3

      - name: Run Playwright Test (backend readiness)
        run: npm run test:playwright

      - name: Build Electron App
        run: npm run build:electron -- --${{ matrix.arch }}
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload Platform Bundle
        uses: actions/upload-artifact@v4
        with:
          name: pyms-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            public/backend/
            dist/
            release/
            out/
            *.exe
            *.AppImage
            *.dmg

  release:
    needs: build-platform
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: bundles/

      - name: List Bundles
        run: |
          echo "📦 Final App Bundles:"
          ls -R bundles/
